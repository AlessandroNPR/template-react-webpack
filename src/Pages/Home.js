import React, {Fragment} from 'react'
import {Container, Form, FormGroup, Input, Button, Label, FormText} from "reactstrap";
import '../assets/css/sidebar.css'
import Sidebar from '../Components/Sidebar';

const Home = (props) => {
    let {
        width
    } = props
    return <Fragment>

        </Fragment>
}
export default Home